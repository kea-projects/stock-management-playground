export const BaseConfig = null /*
we will need to use it when we will start to agree on endpoints and have some swagger docs
new Configuration({
    basePath:
        process.env.NODE_ENV !== 'test'
            ? process.env.REACT_APP_API_URL
            : 'undefined',
})*/
