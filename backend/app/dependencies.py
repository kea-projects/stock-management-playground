# We are going to need some dependencies used in several places of
# the application.
# So we put them in their own dependencies module (app/dependencies.py).
# An example would be a function that reads a token from the header.
